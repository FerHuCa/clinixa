# Plan HUE-04 / HUE-08-dir / HUE-09 — Avatar + directorio y perfiles públicos

**Fecha:** 2026-06-17 · **Diseño:** Opus · **Ejecución:** 1 agente backend (fase 1) + 3 agentes frontend en paralelo (fase 2)

## Hallazgos previos (ya verificados)

- **HUE-08 (signup público) YA FUNCIONA** end-to-end: `/bienvenida` → `/sign-up` (rol) → Clerk `<SignUp unsafeMetadata={{role}}>` → backend lee `unsafe_metadata.role` (`Program.cs:4087,4133`) y crea usuario con ese rol → onboarding → `/activacion`. **No se reimplementa.** El tercer agente construye el **directorio público** (la superficie de descubrimiento que faltaba).
- **No hay `dotnet ef`** (manifest vacío) → **CERO migraciones**. El avatar usa el campo `ProfilePhotoUrl` que **ya existe** en `Professional`. El slug se **computa en el DTO** (sin columna).
- `GET /api/professionals` (listado, `Program.cs:1516`) y `GET /api/professionals/{id}` (`:1557`) ya son **públicos** (`IsPublicApiRequest` cubre GET `/api/professionals`) y filtran `Status=="active" && VerificationStatus=="verified"`.
- Tipo TS `Professional` = inferido de `apps/web/lib/demo-data.ts` (`professionals`). Headers de auth: `getAuthHeaders()` de `lib/auth-client.ts`. Base API: `API_BASE_URL` (`NEXT_PUBLIC_API_BASE_URL`).

---

# FASE 1 — Backend + capa compartida (1 agente, archivos exclusivos)

**Archivos que posee:** `apps/api/Program.cs`, `apps/api/Contracts/ApiContracts.cs`, `apps/api/Infrastructure/MappingExtensions.cs`, `apps/web/lib/healthhub-store.ts`, `apps/web/lib/demo-data.ts`, **NUEVO** `apps/web/lib/public-professionals.ts`, **NUEVO** `apps/api/wwwroot/uploads/avatars/.gitkeep`.

### 1A. `ProfessionalDto` — agregar `Slug` y `ProfilePhotoUrl`

`ApiContracts.cs:64`. Agregar **al final** del record (después de `Availability`):
```csharp
    IReadOnlyList<ProfessionalAvailabilityDto> Availability,
    string Slug,
    string ProfilePhotoUrl);
```

### 1B. `MappingExtensions` — Slugify + mapear los 2 campos

Agregar helper (junto a `RemoveDiacritics`):
```csharp
public static string Slugify(string displayName, string id)
{
    var baseSlug = RemoveDiacritics((displayName ?? string.Empty).ToLowerInvariant());
    var sb = new System.Text.StringBuilder();
    foreach (var ch in baseSlug)
    {
        if (char.IsLetterOrDigit(ch)) sb.Append(ch);
        else if (ch is ' ' or '-' or '_' or '.') sb.Append('-');
    }
    var kebab = System.Text.RegularExpressions.Regex.Replace(sb.ToString(), "-+", "-").Trim('-');
    if (string.IsNullOrEmpty(kebab)) kebab = "profesional";
    // Sufijo estable de 6 hex derivado del Id (unicidad sin columna ni migración).
    using var sha = System.Security.Cryptography.SHA256.Create();
    var hash = sha.ComputeHash(System.Text.Encoding.UTF8.GetBytes(id ?? string.Empty));
    var suffix = Convert.ToHexString(hash)[..6].ToLowerInvariant();
    return $"{kebab}-{suffix}";
}
```
En `ToDto` (`MappingExtensions.cs:85`), agregar como últimos 2 argumentos del `new ProfessionalDto(...)`:
```csharp
            ...
                .ToList(),
            Slugify(professional.DisplayName, professional.Id),
            professional.ProfilePhotoUrl ?? "");
```

### 1C. Endpoint público `GET /api/professionals/by-slug/{slug}`

Agregar **junto a** `GET /api/professionals/{id}` (`Program.cs:1557`), antes o después de ese handler:
```csharp
professionalsApi.MapGet("/by-slug/{slug}", async (string slug, HealthHubDbContext db) =>
{
    var candidates = await db.Professionals
        .AsNoTracking()
        .AsSplitQuery()
        .Include(item => item.Services)
        .Include(item => item.Availability)
        .Include(item => item.Reviews)
        .Where(item => item.Status == "active" && item.VerificationStatus == "verified")
        .ToListAsync();

    var match = candidates.FirstOrDefault(item =>
        MappingExtensions.Slugify(item.DisplayName, item.Id) == slug);

    return match is null ? Results.NotFound() : Results.Ok(match.ToDto());
});
```
> `by-slug` es GET bajo `/api/professionals` → ya público vía `IsPublicApiRequest`. **No tocar** `IsPublicApiRequest`. ⚠️ Registrar `/by-slug/{slug}` **antes** que `/{id}` no es necesario (rutas literales tienen prioridad sobre parámetros en ASP.NET), pero verifica que `/{id}` no capture `by-slug`. Si hay duda, registra `by-slug` primero.

### 1D. Avatar: storage local + endpoint de subida

1. **Static files.** En `Program.cs`, después de construir `app` (junto a otros middlewares, antes del middleware de auth de `/api`), agregar:
   ```csharp
   app.UseStaticFiles();
   ```
   (Sirve `apps/api/wwwroot/` en `/`. Los archivos en `/uploads/...` son no-`/api` → el middleware de auth (`Program.cs:145`) ya los deja pasar.)

2. **Carpeta.** Crear `apps/api/wwwroot/uploads/avatars/.gitkeep` (vacío) para versionar el directorio.

3. **Endpoint** (junto a los demás de `professionalPortalApi`, p.ej. tras `PATCH /profile`):
   ```csharp
   professionalPortalApi.MapPost("/avatar", async (HttpRequest request, IWebHostEnvironment env, HealthHubDbContext db) =>
   {
       var actor = await GetUserFromRequestAsync(request, db);
       if (actor is null) return Results.Unauthorized();
       if (actor.Professional is null) return Results.StatusCode(StatusCodes.Status403Forbidden);

       if (!request.HasFormContentType) return Results.BadRequest(new { errors = new[] { "Se esperaba multipart/form-data." } });
       var form = await request.ReadFormAsync();
       var file = form.Files.GetFile("file");
       if (file is null || file.Length == 0) return Results.BadRequest(new { errors = new[] { "Falta el archivo 'file'." } });
       if (file.Length > 2 * 1024 * 1024) return Results.BadRequest(new { errors = new[] { "La imagen no debe exceder 2 MB." } });

       var ext = Path.GetExtension(file.FileName).ToLowerInvariant();
       var allowed = new[] { ".jpg", ".jpeg", ".png", ".webp" };
       if (!allowed.Contains(ext)) return Results.BadRequest(new { errors = new[] { "Formato no soportado (usa JPG, PNG o WEBP)." } });

       var professional = await db.Professionals.FirstAsync(p => p.Id == actor.Professional.Id);
       var webRoot = string.IsNullOrEmpty(env.WebRootPath) ? Path.Combine(env.ContentRootPath, "wwwroot") : env.WebRootPath;
       var dir = Path.Combine(webRoot, "uploads", "avatars");
       Directory.CreateDirectory(dir);
       // Nombre estable por profesional; sufijo de versión para cache-busting.
       var fileName = $"{professional.Id}{ext}";
       var fullPath = Path.Combine(dir, fileName);
       using (var stream = File.Create(fullPath))
       {
           await file.CopyToAsync(stream);
       }
       var now = DateTimeOffset.UtcNow;
       professional.ProfilePhotoUrl = $"/uploads/avatars/{fileName}?v={now.ToUnixTimeSeconds()}";
       professional.UpdatedAt = now;
       AddAuditLog(db, request, actor, "professional.avatar.update", "professional", professional.Id, null, professional.Id);
       await db.SaveChangesAsync();

       var refreshed = await db.Professionals.AsNoTracking().AsSplitQuery()
           .Include(p => p.Services).Include(p => p.Availability).Include(p => p.Reviews)
           .FirstAsync(p => p.Id == professional.Id);
       return Results.Ok(refreshed.ToDto());
   });
   ```
   > Verifica que `actor.Professional` esté cargado en `GetUserFromRequestAsync` (si no, carga el profesional por `UserId`). Ajusta el patrón al de otros handlers de `professionalPortalApi` (revisa cómo obtienen el `Professional` actual, p.ej. `PATCH /profile`).

### 1E. Frontend store — tipo + acción de subida

`apps/web/lib/demo-data.ts`: a **cada** objeto de `professionals` agregar dos campos (para que el tipo inferido los incluya):
```ts
    slug: "<kebab-del-nombre>-<6hex-cualquiera>",  // p.ej. "dra-laura-vega-a1b2c3"
    profilePhotoUrl: "",
```
(El valor de `slug` en demo no necesita coincidir con el backend; solo da forma al tipo. `profilePhotoUrl: ""`.)

`apps/web/lib/healthhub-store.ts`:
- Agregar helper de subida multipart (junto a `apiPost`):
  ```ts
  async function apiUpload<T>(path: string, formData: FormData) {
    const authHeaders = await getAuthHeaders(); // NO fijar Content-Type: el browser pone el boundary
    const response = await fetch(`${API_BASE_URL}${path}`, { body: formData, headers: authHeaders, method: "POST" });
    if (!response.ok) throw await createApiError(response, `POST ${path} failed with ${response.status}`);
    return response.json() as Promise<T>;
  }
  ```
- Agregar acción al store (junto a `updateProfessionalProfile`):
  ```ts
  async uploadProfessionalAvatar(file: File) {
    const formData = new FormData();
    formData.append("file", file);
    const updated = await apiUpload<Professional>("/api/professional-portal/avatar", formData);
    setApiStatus("connected");
    return updated;
  },
  ```
  Declararla también en la interfaz/tipo del store si existe una firma explícita de acciones.

### 1F. `apps/web/lib/public-professionals.ts` (NUEVO) — fetch server-side para páginas públicas

```ts
const API_BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://127.0.0.1:5050";

export type PublicProfessionalService = {
  id: string; name: string; description: string; durationMinutes: number; price: number; mode: string;
};
export type PublicProfessionalAvailability = {
  id: string; weekday: number; startsAt: string; endsAt: string;
};
export type PublicProfessional = {
  id: string; displayName: string; specialty: string; specialtyLabel: string; bio: string;
  location: string; appointmentMode: string; basePrice: number; status: string; verificationStatus: string;
  whatsappNumber: string; nextAvailable: string; averageRating: number; reviewCount: number;
  services: PublicProfessionalService[]; availability: PublicProfessionalAvailability[];
  slug: string; profilePhotoUrl: string;
};

function withBase(url: string) {
  // Las URLs de avatar vienen relativas a la API ("/uploads/..."). Prefijar para <img>.
  return url && url.startsWith("/") ? `${API_BASE_URL}${url}` : url;
}

export async function fetchPublicProfessionals(params?: { specialty?: string; query?: string }): Promise<PublicProfessional[]> {
  const search = new URLSearchParams();
  if (params?.specialty && params.specialty !== "all") search.set("specialty", params.specialty);
  if (params?.query) search.set("query", params.query);
  const qs = search.toString();
  const res = await fetch(`${API_BASE_URL}/api/professionals${qs ? `?${qs}` : ""}`, { cache: "no-store" });
  if (!res.ok) return [];
  const list = (await res.json()) as PublicProfessional[];
  return list.map((p) => ({ ...p, profilePhotoUrl: withBase(p.profilePhotoUrl) }));
}

export async function fetchProfessionalBySlug(slug: string): Promise<PublicProfessional | null> {
  const res = await fetch(`${API_BASE_URL}/api/professionals/by-slug/${encodeURIComponent(slug)}`, { cache: "no-store" });
  if (!res.ok) return null;
  const p = (await res.json()) as PublicProfessional;
  return { ...p, profilePhotoUrl: withBase(p.profilePhotoUrl) };
}

export const WEEKDAY_LABELS = ["Domingo", "Lunes", "Martes", "Miércoles", "Jueves", "Viernes", "Sábado"];
```

### Verificación fase 1
```bash
npm run build:api        # 0 errores
cd apps/web && npx tsc --noEmit   # limpio (los 3 consumidores aún no existen; el store/lib sí compilan)
# Reiniciar API y probar:
pkill -f "HealthHub.Api"; npm run dev:api > /tmp/clinixa-api.log 2>&1 &  # ~12s
# by-slug (usar el slug del profesional verificado; obtenerlo del listado):
curl -s "http://localhost:5050/api/professionals" | python3 -c "import sys,json;[print(p['slug'],'->',p['displayName']) for p in json.load(sys.stdin)]"
curl -s "http://localhost:5050/api/professionals/by-slug/<SLUG>" -o /dev/null -w "%{http_code}\n"   # 200
# avatar (multipart):
curl -s -X POST http://localhost:5050/api/professional-portal/avatar \
  -H "X-HealthHub-Dev-User: usr-murcielagolambo-gmail-com" -F "file=@/ruta/a/imagen.png" | python3 -m json.tool | grep profilePhotoUrl
npm run smoke:api        # 31/31
```

---

# FASE 2 — Frontend (3 agentes en PARALELO, archivos exclusivos)

Todos contra el contrato de Fase 1. **Páginas públicas = sin `AppShell`** (no requieren sesión); estilo standalone como `/bienvenida`. **Ningún agente crea `app/profesionales/layout.tsx`** (evita colisión).

## Agente HUE-09 — Perfil público individual
**Posee:** `apps/web/app/profesionales/[slug]/page.tsx`, `apps/web/app/profesionales/[slug]/not-found.tsx`.

- **Server Component.** `export async function generateMetadata({ params })` → usa `fetchProfessionalBySlug` para `title` = `"{displayName} · {specialtyLabel} | Clinixa"`, `description` = primeras ~150 chars del bio, `openGraph` (title/description, `images: [profilePhotoUrl]` si existe).
- `page.tsx`: `const pro = await fetchProfessionalBySlug(params.slug); if (!pro) notFound();`
- Render (público, responsive, Tailwind; reusar tokens visuales del proyecto: `border-border`, `text-primary`, `bg-teal-50`, etc.):
  - **Hero**: avatar (`<img>` de `pro.profilePhotoUrl` o placeholder con iniciales si vacío), nombre, `specialtyLabel`, `location`, rating (`averageRating`/`reviewCount`).
  - **Bio**, **Servicios** (nombre, duración, precio MXN, modo), **Disponibilidad** (usar `WEEKDAY_LABELS[weekday]` + `startsAt`–`endsAt`), **Reviews** (si llegan).
  - **CTAs**: WhatsApp (`https://wa.me/{whatsappNumber sin +/espacios}` si existe) + "Crear cuenta para agendar" → `/sign-up`.
  - Header simple con logo → link a `/profesionales`. Footer `LegalFooter` (existe en `components/legal-footer`).
- `not-found.tsx`: mensaje "Profesional no encontrado" + link a `/profesionales`.

## Agente HUE-08-dir — Directorio público
**Posee:** `apps/web/app/profesionales/page.tsx`, **NUEVO** `apps/web/components/professional-card.tsx`.

- **Server Component** con `searchParams`: `?especialidad=&q=`. `generateMetadata` estático ("Directorio de profesionales | Clinixa").
- `const pros = await fetchPublicProfessionals({ specialty: searchParams.especialidad, query: searchParams.q });`
- **Filtro por especialidad**: chips usando `SPECIALTY_OPTIONS` de `@/lib/specialty-labels` + opción "Todas". Cada chip es un `<Link href={`/profesionales?especialidad=${value}${q?...:''}`}>` (navegación por URL, SEO-friendly, sin estado cliente).
- **Búsqueda**: un `<form method="get">` con `<input name="q">` (server-side, sin JS). Conserva `especialidad` con un hidden input.
- **Grid** de `<ProfessionalCard pro={...} />` enlazando a `/profesionales/{pro.slug}`.
- `components/professional-card.tsx`: avatar/placeholder, nombre, specialtyLabel, location, rating, precio base "desde $X", chip de modalidad. Componente presentacional (recibe `PublicProfessional`). Puede ser server o `"use client"` solo si necesita interacción (no la necesita).
- Header + `LegalFooter` como `/bienvenida`. Empty state si `pros.length === 0`.

## Agente HUE-04 — Avatar en el portal
**Posee:** `apps/web/app/portal-profesional/professional-portal-page-client.tsx`.

- Importar `uploadProfessionalAvatar` del store.
- En la sección de perfil (cerca del input de cédula/displayName), agregar bloque **"Foto de perfil"**:
  - Mostrar avatar actual: `<img>` de `professional?.profilePhotoUrl` (prefijar con `apiBaseUrl` del store si es relativo: el store expone `apiBaseUrl`; o usar `process.env.NEXT_PUBLIC_API_BASE_URL`). Si vacío → placeholder con iniciales.
  - `<input type="file" accept="image/png,image/jpeg,image/webp">` + botón "Subir foto".
  - Al seleccionar: validar tamaño ≤2MB en cliente, mostrar preview (`URL.createObjectURL`), llamar `await uploadProfessionalAvatar(file)`, actualizar el estado local del profesional con la respuesta (incluye `profilePhotoUrl` nuevo con `?v=` cache-bust). Manejar loading/error.
  - Nota UX: "JPG, PNG o WEBP, máx 2 MB. Tu foto aparecerá en tu perfil público."
- No tocar otros archivos. Si el objeto `professional` del portal no trae `profilePhotoUrl` en su tipo, ya lo agregó Fase 1 al tipo `Professional`.

### Verificación fase 2 (la corre el orquestador tras los 3 agentes)
```bash
npm run lint:web && cd apps/web && npx tsc --noEmit   # limpios
# build:web NO con dev server activo. Si se corre, detener Next antes.
```
Verificación visual (usuario): `/profesionales`, `/profesionales/{slug}`, y subida de avatar en `/portal-profesional`.

---

## Fuera de alcance
- Azure Blob (producción): el storage local es puente para el piloto; migrar a Blob es cambiar solo el endpoint de avatar.
- Paginación del directorio (volumen bajo). Recorte/resize de imagen server-side.
- No tocar `IsPublicApiRequest`, `canPublish`, ni el gate de verificación.
